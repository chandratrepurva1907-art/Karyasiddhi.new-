karyasiddhi is my first ever web project built using the following-
python for backend and html,css, javascript for frontend 

* **Web Interface:** Built with Flask and styled with custom templates.
* **User Data Management:** Stores information locally in `user_data.json`.
* **Configurable:** Settings managed via `.vscode` and environment-specific files.
